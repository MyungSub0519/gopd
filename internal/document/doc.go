// Package document owns PDF byte snapshots, source ranges, object lookup,
// cross-reference sections and decoded streams. It uses syntax to parse object
// values and leaves page content interpretation to its callers.
package document

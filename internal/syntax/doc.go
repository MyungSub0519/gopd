// Package syntax turns PDF bytes into model objects. The scanner produces
// tokens and the parser assembles them into direct objects and references.
// It knows nothing about files, xrefs or page contents.
package syntax

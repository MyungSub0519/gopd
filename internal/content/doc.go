// Package content interprets page contents. The semantic builder walks the page
// tree and resolves resources, the interpreter executes content-stream
// operators against a graphics state, and fonts and CMaps decode glyph codes.
// It builds the detailed result and projects the basic one from it.
package content

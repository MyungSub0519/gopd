// Package model holds the PDF object and file-structure types shared by every
// layer: spans and sources, object values, indirect objects, xref entries and
// the parsing limits. It carries data and pure accessors only; the reading,
// decoding and interpretation logic lives in the layer packages above it.
package model
